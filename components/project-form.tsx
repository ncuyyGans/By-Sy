import type { Project } from "@/lib/data";

export function ProjectForm({ action, project, error }: { action: (formData: FormData) => Promise<void>; project?: Project; error?: string }) {
  return <form action={action} className="admin-card form-grid">{error && <div className="form-error">Project belum berhasil disimpan. Periksa field wajib lalu coba lagi.</div>}{project && <input type="hidden" name="id" value={project.id} />}<label>Nama project<input name="title" defaultValue={project?.title} required /></label><label>Label / jenis project<input name="eyebrow" defaultValue={project?.eyebrow ?? "Project"} placeholder="Personal project, client work, experiment..." /></label><div className="two-columns"><label>Tahun<input name="year" defaultValue={project?.year ?? String(new Date().getFullYear())} /></label><label>Urutan<input name="sort_order" type="number" defaultValue={project?.sort_order ?? 0} /></label></div><label>Deskripsi<textarea name="description" defaultValue={project?.description} required /></label><label>Link project<input name="url" type="url" defaultValue={project?.url} placeholder="https://" required /></label><label>Link repository<input name="repo_url" type="url" defaultValue={project?.repo_url ?? ""} placeholder="https://github.com/..." /></label><label>Tags teknologi<input name="tags" defaultValue={project?.tags?.join(", ")} placeholder="Next.js, Supabase, Writing" /></label><label className="checkbox-field"><input name="visible" type="checkbox" defaultChecked={project?.visible ?? true} /> Tampilkan di halaman Projects</label><div className="form-actions"><button className="button" type="submit">{project ? "Simpan project" : "Tambah project"}</button><a className="text-link" href="/admin">Batal</a></div></form>;
}
